<p id="notice"><%= notice %></p>

<h1>Fitnessplanners</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fitnessplanners.each do |fitnessplanner| %>
      <tr>
        <td><%= link_to 'Show', fitnessplanner %></td>
        <td><%= link_to 'Edit', edit_fitnessplanner_path(fitnessplanner) %></td>
        <td><%= link_to 'Destroy', fitnessplanner, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Fitnessplanner', new_fitnessplanner_path %>
